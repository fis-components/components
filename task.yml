sync:
  npm:
    # 当不指定版本时，默认同步最新的3个版本。
    # 当版本为 latest 时只会同步最新的那个版本。
    # 可以同时处理多个组件。
    # - whatwg-fetch
    # - react@0.14.5	
    # 不要删除现有的，免得冲突 merge 不了。工具会自动 diff 只处理本次更新的部分。
    - fit-auto-complete@latest
    - fit-button@latest
    - fit-checkbox@latest
    - fit-collapse@latest
    - fit-datepicker@latest
    - fit-iframe@latest
    - fit-image@latest
    - fit-input@latest
    - fit-json-tree@latest
    - fit-layout@latest
    - fit-layout-global@latest
    - fit-menu@latest
    - fit-message@latest
    - fit-modal@latest
    - fit-number@latest
    - fit-pagination@latest
    - fit-phone@latest
    - fit-radio@latest
    - fit-render-to@latest
    - fit-select@latest
    - fit-style@latest
    - fit-switch@latest
    - fit-table@latest
    - fit-tabs@latest
    - fit-timepicker@latest
    - fit-upload@latest
    - color
    - fit-tree@latest
    - fiten-chat-box@latest
    - fiten-chat@latest
    - react-tap-event-plugin@latest
