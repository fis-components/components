sync:
  npm:
    # 当不指定版本时，默认同步最新的3个版本。
    # 当版本为 latest 时只会同步最新的那个版本。
    # 可以同时处理多个组件。
    # - whatwg-fetch
    # - react@0.14.5
    # 每次只保留当前需要转换的组件。
    - fit-button
    - fit-captcha
    - fit-checkbox
    - fit-collapse
    - fit-input
    - fit-datepicker
    - fit-layout
    - fit-layout-global
    - fit-menu
    - fit-message
    - fit-modal
    - fit-number
    - fit-pagination
    - fit-radio
    - fit-select
    - fit-style
    - fit-switch
    - fit-table
    - fit-tabs
    - fit-timepicker
