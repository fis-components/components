sync:
  npm:
    # 当不指定版本时，默认同步最新的3个版本。
    # 当版本为 latest 时只会同步最新的那个版本。
    # 可以同时处理多个组件。
    # - whatwg-fetch
    # - react@0.14.5	
    # 不要删除现有的，免得冲突 merge 不了。工具会自动 diff 只处理本次更新的部分。
    - fit-table@1.1.59
    - assertion-error@1.0.1
    - type-detect@0.1.1
    - react-onclickout@latest
